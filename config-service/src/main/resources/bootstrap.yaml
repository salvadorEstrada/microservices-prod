server:
  port:8081:
spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:salvadorEstrada/micro-services.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user: root
      name: s3cr3t

